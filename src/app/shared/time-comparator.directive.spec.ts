import { TimeComparatorDirective } from './time-comparator.directive';

describe('TimeComparatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeComparatorDirective();
    expect(directive).toBeTruthy();
  });
});
